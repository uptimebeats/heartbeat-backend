version: '3.8'

services:
  heartbeat-api:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - DATABASE_URL=${DATABASE_URL}
      - API_SECRET=${API_SECRET}
    restart: unless-stopped
